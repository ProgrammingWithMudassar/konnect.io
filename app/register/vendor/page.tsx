import Vendor from "@/views/Register/Vendor";

export const metadata = {
    title: 'Welcome | Konnect.io',
    description: 'Welcome | Konnect.io',
}

const page = () => {
    return (
        <Vendor />
    )
}

export default page;